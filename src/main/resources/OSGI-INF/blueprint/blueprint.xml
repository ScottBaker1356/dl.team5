<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd        http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

    <bean id="routeLogger" class="com.team5.dl.ProcessLogger"/>
    <bean id="exceptionHandler" class="com.team5.dl.ExceptionHandler"/>

    <bean id="serviceProcessor" class="com.team5.dl.CamelProcessor"/>

    <bean id="jasypt" class="org.apache.camel.component.jasypt.JasyptPropertiesParser">
        <property name="password" value="qLVx'hf=dLNP!LQR"/>
    </bean>

    <camelContext id="team5DomainLayerCamelContext" useMDCLogging="true"
                  xmlns="http://camel.apache.org/schema/blueprint">

        <propertyPlaceholder id="camelContextConfigProps" location="app.properties" propertiesParserRef="jasypt"/>

        <onException>
            <exception>java.lang.Exception</exception>
            <handled>
                <constant>true</constant>
            </handled>
            <bean method="process" ref="exceptionHandler"/>
        </onException>

        <restConfiguration bindingMode="off" component="servlet" contextPath="/api" port="{{jettyport}}">
            <dataFormatProperty key="prettyPrint" value="true"/>
        </restConfiguration>

        <rest path="/domain-layer">
            <post uri="/notify-of-event">
                <to uri="direct:serviceProcessor"/>
            </post>
            <post uri="/send-text-message">
                <to uri="direct:serviceProcessor"/>
            </post>
            <get uri="/sensor-event-preferences/{id}">
                <to uri="direct:serviceProcessor"/>
            </get>
            <put uri="/sensor-event-preferences/{id}">
                <to uri="direct:serviceProcessor"/>
            </put>
        </rest>

        <route id="serviceProcessor" streamCache="true">
            <from uri="direct:serviceProcessor"/>
            <process ref="routeLogger"/>
            <setProperty propertyName="karaf.host">
                <simple>{{karaf.host}}</simple>
            </setProperty>
            <setProperty propertyName="aws.access.key">
                <simple>{{aws.access.key}}</simple>
            </setProperty>
            <setProperty propertyName="aws.secret.key">
                <simple>{{aws.secret.key}}</simple>
            </setProperty>
            <setProperty propertyName="aws.service.endpoint">
                <simple>{{aws.service.endpoint}}</simple>
            </setProperty>
            <setProperty propertyName="aws.signing.region">
                <simple>{{aws.signing.region}}</simple>
            </setProperty>
            <process ref="serviceProcessor"/>
            <process ref="routeLogger"/>
        </route>

    </camelContext>

</blueprint>